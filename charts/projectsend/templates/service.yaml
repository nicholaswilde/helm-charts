{{- if .Values.service -}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "projectsend.fullname" . }}
  labels:
    {{- include "projectsend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "projectsend.selectorLabels" . | nindent 4 }}
{{- end }}
