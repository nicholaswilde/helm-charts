{{- if .Values.service -}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud9.fullname" . }}
  labels:
    {{- include "cloud9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud9.selectorLabels" . | nindent 4 }}
{{- end }}
