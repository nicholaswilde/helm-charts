---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "papermerge.fullname" . }}
  labels:
    {{- include "papermerge.labels" . | nindent 4 }}
{{- with .Values.config }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
